/**
 * Custom branding configuration override.
 * 
 * This file can be customized per client to override the default branding.
 * If this file exists and exports a `brandingConfig`, it will be merged with
 * the default branding configuration.
 * 
 * Example:
 * ```typescript
 * import { AuthBrandingConfig } from "@/lib/core/config/branding"
 * 
 * export const brandingConfig: Partial<AuthBrandingConfig> = {
 *   colors: {
 *     primary: "#custom-color",
 *   },
 *   texts: {
 *     loginTitle: "Custom Login Title",
 *   },
 * }
 * ```
 */

// By default, this file exports nothing (uses default branding)
// Override this file in custom branches to provide custom branding
