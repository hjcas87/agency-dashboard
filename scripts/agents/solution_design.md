# Agent Starter — Solution Design

You are creating solution design documentation for a **Web Application Boilerplate** client project.

## Always do first (MANDATORY)
1) Read:
   - `AGENTS.md`
   - `ARCHITECTURE.md`
   - `docs/PROJECT_BRIEF.md` (if exists)
2) Identify:
   - Client needs from PROJECT_BRIEF
   - Scope and requirements
   - Constraints and assumptions

---

## Role selection (MANDATORY)
Use role:
- `docs/agents/roles/solution_designer.md`

Follow its authority and constraints strictly.

---

## Skill triggers (MANDATORY)
Apply the Skills Protocol from `AGENTS.md`.

Always use:
- `docs/agents/skills/solution_design.md`

---

## Source of truth (MANDATORY)
You MUST base your work on:
- `docs/PROJECT_BRIEF.md` (created by analyst after client meeting)

If PROJECT_BRIEF doesn't exist, STOP and ask for it.

---

## Solution design rules (STRICT)
- Functional level only (no technical implementation).
- Clear, unambiguous language.
- Suitable for client review and sign-off.
- Each user story maps to one feature (backend + frontend if needed).

---

## What to produce (MANDATORY)
1) Solution design documents in `docs/solution_design/`:
   - `system_requirements.md`
   - `scope.md`
   - `assumptions_and_constraints.md`
   - `user_stories/*.md` (one per user story)
   - `diagrams/*.excalidraw` (if needed)
2) Documents are:
   - Clear and reviewable by non-technical stakeholders
   - Mappable to features by developers
   - Suitable for client sign-off
